# Kafka and Pub/Sub: Deep Conceptual Guide

এই ডকুমেন্টটি মাইক্রোসার্ভিস আর্কিটেকচার, ইভেন্ট-ড্রিভেন ডিজাইন এবং Kafka ব্রোকার ম্যানেজমেন্ট নিয়ে আমাদের বিস্তারিত আলোচনার একটি পূর্ণাঙ্গ রূপ। এখানে প্রতিটি প্রশ্নের উত্তর বিস্তারিত ব্যাখ্যা এবং কোড উদাহরণসহ দেওয়া হয়েছে।

---

### ১. মাইক্রোসার্ভিসে ডাটাবেস ডিজাইন (Distributed Monolith বনাম Database per Service)

**প্রশ্ন:** "Shared Database (Distributed Monolith)" — এই পয়েন্টে তুমি কি বলতে চাচ্ছো যে প্রত্যেকটা সার্ভিসের জন্য আলাদা আলাদা ডেটাবেস হতে হবে?

**উত্তর:** হ্যাঁ, একদম তাই। মাইক্রোসার্ভিসের একদম স্ট্যান্ডার্ড প্র্যাকটিস হচ্ছে **"Database per Service"**। বর্তমানে আপনার প্রজেক্টে সব সার্ভিস একটি মাত্র `schema.prisma` এবং একটি মাত্র MongoDB database শেয়ার করছে, যাকে "Distributed Monolith" বলা হয়। এর সমস্যাগুলো হলো:

- **ডেটা কাপলিং (Tight Coupling):** একটি সার্ভিসের (`auth-service`) প্রয়োজনে মডেলে বড় কোনো চেঞ্জ করলে অন্য সার্ভিসগুলো (`order-service`) ভেঙে যেতে পারে।
- **স্কেলেবিলিটি (Scalability):** `product-service`-এ রিড অপারেশন বেশি হয়, আবার `order-service`-এ রাইট ট্রানজেকশন বেশি। সব এক ডিবিতে থাকলে আপনি সার্ভিস অনুযায়ী ডাটাবেস টিউনিং বা আলাদা টেকনোলজি (যেমন Redis বা Cassandra) ব্যবহার করতে পারবেন না।
- **ইনডিপেন্ডেন্ট ডিপ্লয়মেন্ট:** শেয়ারড ডাটাবেস থাকলে আপনাকে প্রায়ই ডাটাবেস মাইগ্রেশনের জন্য সব সার্ভিস একসাথে ডাউন রাখতে হয়, যা মাইক্রোসার্ভিসের মূল উদ্দেশ্যের পরিপন্থী।
- **ডাটা ওনারশিপ:** আইডিয়াল মাইক্রোসার্ভিসে একটি সার্ভিস সরাসরি অন্য সার্ভিসের টেবিলে কোয়েরি করতে পারে না। ডাটা শেয়ার হতে হবে API বা ইভেন্টের মাধ্যমে।

---

### ২. ডাটাবেস আলাদা হলে রিলেশন ও পপুলেশন হ্যান্ডেলিং

**প্রশ্ন:** এখন ডাটাবেস যদি আলাদা হয় তাহলে রিলেশন মেইনটেইন করবো কিভাবে আর ডাটা পপুলেট (Populate) করবো কিভাবে? ইন্ডাস্ট্রি লেভেলে এগুলো কিভাবে হ্যান্ডেল করে?

**উত্তর:** মাইক্রোসার্ভিসে সরাসরি SQL Join বা Prisma `include` কাজ করে না। ইন্ডাস্ট্রি লেভেলে ৩টি পদ্ধতিতে এটি সমাধান করা হয়:

#### ক. API Composition (Gateway Aggregator)

**API Gateway** বা একটি এগ্রিগেটর সার্ভিস ডাটাগুলোকে কম্বাইন করে।

- **উদাহরণ:** ইউজার তার অর্ডার লিস্ট দেখবে। Gateway প্রথমে `order-service` থেকে অর্ডারের ডাটা আনবে। এরপর সেখান থেকে `productId` নিয়ে `product-service`-কে কল করে প্রোডাক্ট ডিটেইলস আনবে। শেষে সব মিলিয়ে ইউজারকে দেখাবে।

#### খ. Event-Driven Data Syncing (Kafka-র মাধ্যমে)

এটাকে বলা হয় **Data Denormalization**। যেহেতু আপনার প্রজেক্টে Kafka আছে, এটাই বেস্ট সল্যুশন।

- **উদাহরণ:** `product-service`-এ দাম চেঞ্জ হলে একটি ইভেন্ট পাঠাবে। `order-service` সেই ইভেন্ট শুনে তার নিজের ডাটাবেসে থাকা প্রোডাক্টের কপির দাম আপডেট করে নিবে। এতে জয়েন করার জন্য অন্য সার্ভিসকে আর কল করতে হয় না।

#### গ. CQRS (Command Query Responsibility Segregation)

জটিল সার্চ বা রিপোর্টের জন্য একটি আলাদা **Read-only Database** (যেমন Elasticsearch) রাখা হয় যেখানে সব সার্ভিসের ডাটা Kafka-র মাধ্যমে সিঙ্ক হয়ে জমা থাকে।

---

### ৩. মাল্টিপল সার্ভিস কনজাম্পশন এবং Consumer Group

**প্রশ্ন:** একটা Kafka ইভেন্ট যদি পাস করা হয়, সেটা কি একাধিক সার্ভিস কনজিউম করতে পারে? অনেক সময় আমার একটা কাজের জন্য মাল্টিপল সার্ভিসে কাজ করার প্রয়োজন হতে পারে।

**উত্তর:** হ্যাঁ, এটাই Kafka-র সবচেয়ে বড় শক্তি। এটি **"Consumer Groups"**-এর মাধ্যমে কাজ করে।

**উদাহরণ (Broadcast System):**
ধরুন একটি অর্ডার প্লে হলো এবং `order-service` একটি ইভেন্ট পাঠাল: `order.placed`. এখন একই সাথে নিচের কাজগুলো হওয়া দরকার:

- `inventory-service` (Group: `inventory-group`) -> স্টক কমাবে।
- `email-service` (Group: `email-group`) -> কনফার্মেশন মেইল পাঠাবে।
- `shipping-service` (Group: `shipping-group`) -> ডেলিভারি প্রসেস শুরু করবে।

যেহেতু সবার **Group ID** আলাদা, Kafka অটোমেটিক মেসেজের একটি করে কপি প্রত্যেকটি সার্ভিসকে পৌঁছে দিবে। কেউ কারো জন্য ওয়েট করবে না।

---

### ৪. Producer এবং Consumer-এর দায়িত্ব (Group ID লজিক)

**প্রশ্ন:** মেসেজ প্রোডিউস করার টাইমে কি ইভেন্ট গ্রুপ আইডি দিতে হবে নাকি কনজিউম করার টাইমে দিতে হবে? কনজিউমার কি নিজের মতো নিউ ইউনিক গ্রুপ আইডি সেট করে মেসেজ কনজিউম করতে পারবে? নাকি আগে থেকে প্রাক-নির্ধারিত আইডি হতে হবে?

**উত্তর:** **`groupId` শুধুমাত্র কনজিউমার সাইডে দিতে হয়।** প্রোডিউসার জাস্ট টপিকের নাম ধরে মেসেজ পাঠিয়ে দেয়।

- **Producer:** সে শুধু জানে সে কোন টপিকে (`topic: 'order-events'`) ডাটা পাঠাচ্ছে। কে পড়ছে বা কয়জন পড়ছে সেটা তার দেখার বিষয় না।
- **Consumer:** প্রতিটি সার্ভিস কানেক্ট করার সময় নিজের একটি `groupId` ঘোষণা করে।
- **Dynamic Creation:** এই আইডি আগে থেকে কোথাও রেজিস্টার বা ডিফাইন করতে হয় না। কোড লেভেলে আপনি যখনই নতুন আইডি দিয়ে কনজিউমার স্টার্ট করবেন, Kafka ব্রোকার ডাইনামিকালি সেটি তৈরি করে নিবে।

---

### ৫. নতুন সার্ভিস এবং পুরনো ডাটা কনজাম্পশন (Historical Data)

**প্রশ্ন:** ধরো আমার ২টা সার্ভিস (Inventory ও Order) ২ মাস ধরে ডাটা কনজিউম করছে। এখন আমি একটি নিউ `logger-service` আনলাম এবং একটি নিউ গ্রুপ আইডি দিয়ে কনজিউম করা শুরু করলাম। এই নিউ সার্ভিস কি ২ মাসের সব ডাটা কনজিউম করবে?

**উত্তর:** এটি দুটি টেকনিক্যাল বিষয়ের ওপর নির্ভর করে:

#### ক. Retention Period

Kafka-র ডাটা কতদিন থাকবে সেটির একটি লিমিট থাকে। একে **Retention** বলা হয়।

- যদি আপনার Kafka সার্ভারে রিটেনশন পিরিয়ড ৩ মাস সেট করা থাকে, তবে ডাটাগুলো ব্রোকারে আছে।
- যদি রিটেনশন ৭ দিন হয়, তবে ২ মাস আগের ডাটা ডিলিট হয়ে গেছে।

#### খ. Auto Offset Reset (`fromBeginning`)

যদি ডাটা ব্রোকারে থেকে থাকে, তবে নতুন সার্ভিসটি চালু করার সময় আপনাকে একটি কনফিগ সেট করতে হবে:

```javascript
await consumer.subscribe({
  topic: "user-order",
  fromBeginning: true, // অথবা configuration এ earliest সেট করা
});
```

**ফলাফল:** আপনি যদি `fromBeginning: true` দেন এবং ডাটা রিটেনশন পিরিয়ডে থাকে, তবে নতুন সার্ভিসটি চালু হওয়া মাত্রই সে ২ মাস আগের প্রথম মেসেজ থেকে পড়া শুরু করবে এবং কয়েক সেকেন্ডের মধ্যে সব পুরনো ডাটা প্রসেস করে ফেলবে।

---

### ৬. ব্রোকার সাইডে কনফিগারেশন

**প্রশ্ন:** Kafka Broker কনফিগারেশনে কি আমার `groupId` আগে থেকে ডিফাইন করার প্রয়োজন আছে?

**উত্তর:** **না।** ব্রোকার সাইডে গ্রুপ আইডি ডিফাইন করার কোনো অপশন বা প্রয়োজন নেই।

- ব্রোকার শুধু ডাটা স্টোর করে।
- গ্রুপ আইডিগুলো কনজিউমাররা নিজেরা নিয়ে আসে।
- ব্রোকার শুধু মনে রাখে—"Logger গ্রুপটি এই টপিকের ৫০০ নম্বর মেসেজ পর্যন্ত পড়েছে"। এরপর যখনই Logger সার্ভিস রিস্টার্ট হবে, ব্রোকার তাকে ৫০১ নম্বর মেসেজ থেকে ডাটা দেওয়া শুরু করবে।

---

### কোড উদাহরণ (Practical Setup)

**Producer (Send Only):**

```javascript
await producer.send({
  topic: "user-order",
  messages: [{ value: JSON.stringify({ orderId: "123", status: "placed" }) }],
});
```

**Consumer (Receive and Scale):**

```javascript
const consumer = kafka.consumer({ groupId: "my-new-logger-service" }); // ইউনিক আইডি
await consumer.connect();
await consumer.subscribe({ topic: "user-order", fromBeginning: true });

await consumer.run({
  eachMessage: async ({ message }) => {
    console.log("Processing Data:", message.value.toString());
  },
});
```

### সারসংক্ষেপ উপমা (Analogy):

**Kafka একটি নিউজপেপার সার্ভিসের মতো।**

- **Writer (Producer):** খবর লেখে এবং "খেলাধুলা" ক্যাটাগরিতে (Topic) জমা দেয়। সে জানে না কে পড়ছে।
- **Reader (Consumer):** আপনি যখনই এই পত্রিকার "গ্রাহক" হন (Unique GroupID দিয়ে Subscribe করেন), তখনই আপনি খবর পেতে শুরু করেন। আপনি চাইলে পুরনো আর্কাইভ থেকেও খবর পড়তে পারেন যদি লাইব্রেরিতে (Retention) সেগুলো থাকে।
